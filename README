pdo_oci8
########

A PDO userspace driver for the Oracle database using the oci8 PHP module.

Based on `ramsey/pdo_oci8` and `yjeroen/oci8Pdo`.

Example usage:

~~~
$conn = new nineinchnick\pdo\Oci8($dsn, $username, $password, $attributes);
~~~
